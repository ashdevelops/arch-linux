{
    "output": "HDMI-A-1",
    "layer": "top",
    "height": 35,
    "spacing": 0,
    "modules-left": ["hyprland/workspaces", "hyprland/submap", "hyprland/scratchpad"],
    "modules-center": ["hyprland/window"],
    "modules-right": ["disk", "disk#primary", "keyboard-state", "wireplumber", "cpu", "memory", "clock", "tray"],

    "hyprland/workspaces": {
        "all-outputs": true
    },

    "disk": {
        "interval": 30,
        "format": "{path}",
        "path": "/mnt/storage"
    },

    "disk#primary": {
        "interval": 30,
        "format": "{path}",
        "path": "/"
    },

    "keyboard-state": {
        "numlock": false,
        "capslock": true,
        "format": "{name} {icon}",
        "format-icons": {
            "locked": "",
            "unlocked": ""
        }
    },

    "hyprland/scratchpad": {
        "format": "{icon} {count}",
        "show-empty": false,
        "format-icons": ["", ""],
        "tooltip": true,
        "tooltip-format": "{app}: {title}"
    },

    "tray": {
        "spacing": 12
    },

    "clock": {
        "interval": 1,
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "format": "{:%H:%M:%S}",
        "format-alt": "{:%Y-%m-%d}"
    },

    "cpu": {
        "format": "{usage}% ",
        "tooltip": false
    },

    "memory": {
        "format": "{}% "
    },
    
    "wireplumber": {
        "format": "{icon} {volume}%",
        "format-muted": "",
        "on-click": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
        "format-icons": [" ", " ", " "],
        "max-volume": 150,
        "scroll-step": 2
    }
}
